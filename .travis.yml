dist: trusty
sudo: false
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-5
            - google-mock

script:
    - CXX=/usr/bin/g++-5
    - CC=/usr/bin/gcc-5
    - mkdir build
    - cd build
    - cmake ..
    - make -j2
