dist: trusty
sudo: required
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-7

script:
    - CXX=/usr/bin/g++-7
    - CC=/usr/bin/gcc-7
    - mkdir build
    - cd build
    - cmake ..
    - make -j4
